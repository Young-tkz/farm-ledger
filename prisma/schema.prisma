datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")           // Changed from POSTGRES_PRISMA_URL
  directUrl = env("DATABASE_URL_UNPOOLED")  // Changed from POSTGRES_URL_NON_POOLING
}
