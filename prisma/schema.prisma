generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_PRISMA_URL") // Vercel will fill this in automatically
  directUrl = env("POSTGRES_URL_NON_POOLING") // Vercel uses this for migrations
}